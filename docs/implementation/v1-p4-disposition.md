# V1-P4 normal no-effect disposition

The #23 boundary records Work Unit success from the current P3 semantic
acceptance and complete retained justification. The same admitted Contract must
explicitly declare an empty required-effect set. The completed-and-verified
effect terms are vacuously satisfied; neither optional effects nor GitDelivery
are executed.

Normal finalization owns the interval from live runtime observation through P3
custody to durable disposition. A per-Attempt host file lock outside the disposable
worktree excludes concurrent finalizers. A durable start record distinguishes a
waiting duplicate from a caller that acquires ownership after interruption.
An existing complete disposition is read back. A previous start without complete
disposition follows abandonment; retained P3 custody cannot restore eligibility.
The lock and start record contain no runtime history or semantic checkpoint.

The finalizer requires a newly captured P3 record during its own uninterrupted
operation. It does not accept arbitrary previously retained custody. P3 continues
to own normal forward observation, structural mutation/final-occurrence provenance,
declared material capture and criterion-level rationale completeness. No new
assessor, semantic admission hop or generic validator is introduced.

Disposition and abandonment compete at the store's existing write transaction.
Abandonment winning prevents success. A complete disposition winning remains
immutable and readable, and late administration cannot turn it into replacement
authority. Interrupted finalization retains a minimal abandonment reason and
existing trustworthy Attempt/submission/custody references. Physical cessation
and retirement still use #21; failure to prove cessation continues to block #22.

Disposition references immutable P3 custody rather than copying a result archive.
The retained Contract, original sources and final candidate/comparison material,
raw evidence, assessment context and rationale support readback after controlled
fixture cleanup. This introduces no general successful-worktree cleanup policy.

The qualification record for #23 must distinguish deterministic crash/race
controls from the actual-provider repair-to-disposition integration witness.
Passing implementation checks alone does not establish G4-V1. The gate remains
a separate read-only issue, and no P5 work is authorized by this implementation.
