# Issue #18 — Contract evidence and controlled review

**Implementation review: COMPLETE. Issue acceptance: PASS.** This is #18's
input-side integration record, not a G3 verdict or Work Unit disposition.

## Authority and decision

The current [v0.5 target](../../docs/governing/broodling-target-responsibility-boundary-design-v0.5.md)
and [plan](../../docs/governing/broodling-implementation-dependency-plan-v0.5.md),
[G1](../v1-p1/issue-11-g1-v1.md), [G2](../v1-p2/issue-16-g2-v1.md) and completed
[#17](issue-17-assurance-graph.md) govern this implementation.

The user resolved the [recorded missing evidence seam](issue-18-evidence-seam.md)
by authorizing a bounded deterministic Broodling evidence leaf inside the existing
Zeroshot agent occurrence/lifecycle. This is the selected v0.5 graph-local trusted
check, not a new native runtime node or evidence/provenance subsystem. The user
also required explicit structured Contract declarations, no retroactive meaning
for existing revisions, literal argv, read-only/no-network execution, actual raw
observations, fail-closed absence, and separate semantic judgment.

The prior blocked/pre-integration records and every historical W3/W4/G1/G2/#17
record remain unchanged. This successor records the resolved implementation.

## What landed

The [implementation guide](../../docs/implementation/v1-p3-evidence-review.md)
describes the optional immutable `MechanicalEvidence(argv,cwd,materials)` field,
P3 preparation refusal for legacy/unsupported declarations, actual deterministic
leaf, typed `evidenceContent` bindings and Attempt-derived B1 `comparisonBase`.

Only the two evidence nodes bind the fixed deterministic selector. They remain
native agent occurrences. The trusted collector captures literal declared checks
in read-only/no-network Bubblewrap with isolated PID lifetime, outside the check's
PID namespace. Required text files preserve exact UTF-8 bytes including line
endings. Population/check/actual host/mode/exit/stdout/stderr/material reach review
and designated authorities; no processor identifier writes trusted Contract/B1.
Repair retains only Contract, obligation signal and adjudicated directive/correction.

The [exact graph deviation](evidence/issue-18-graph-deviation.json) compares the
pre-integration product `bfc7f2e3888c14d31af59e3dc79f21bee57b05d4` with the resulting
graph. Topology, guards, role kinds, timeouts, repair bound and routing are equal.
The changes are the frozen comparison input, typed evidence payload and their
bindings, governed evidence instructions, and the two fixed runtime connections.
No candidate ordinal or new role, observer, store, session manager or router appears.

## Acceptance evidence

| Obligation | Actual evidence |
| --- | --- |
| Frozen runnable declarations; no free-form execution or retroactivity | `tests/test_mechanical_evidence_contract.py` preserves the pre-change canonical-byte hash, proves immutable new revisions and admission, rejects absent/malformed declarations. Literal shell metacharacters remain arguments/data in actual leaf and SDK controls. |
| Initial and renewed missing evidence fail before review | `missing-initial` and `missing-renewed` in [retained SDK evidence](evidence/issue-18-evidence.json). Both use the actual product evidence leaf and terminate `required_evidence_missing`. |
| Complete evidence and semantic insufficiency discriminate | Same record: valid complete control succeeds; wrong population/host/mode/artifact, contradiction and criterion insufficiency each retain `availability=valid` and reach final `semantic_gap`. Controlled semantic authorities evaluate actual bound content, not scenario names. |
| Structural renewal; forged identifiers non-authoritative | Repaired control observes changed candidate stdout/material only after mutation; frozen Contract/B1/population remain unchanged despite forged diagnostic IDs. No hashes establish applicability. |
| Repair firewall and sticky obligations | #18 repair/sticky controls and [rerun #17 regressions](evidence/issue-18-assurance-regressions.txt) preserve exact three-field repair handoff, widened-binding canary, fresh reviews, designated resolution and exhaustion at three. |
| Actual independent reviewer, valid-material visibility and contamination | [Integrated real reviewer record](evidence/issue-18-integrated-reviewer.json): ten checks PASS, clean and deliberately contaminated real Codex occurrences with actual evidence leaf. Source, comparison, raw and governing-text canaries visible; all six forbidden canaries detected only when deliberately supplied; fresh threads, read-only/isolated/ephemeral/network-off argv, unchanged candidate. |
| Deterministic containment and faithful observation | `tests/test_mechanical_evidence.py`: candidate, sibling and shared Git writes denied; network denied; ambient environment removed; exact CRLF; missing/escaped/nontext/unexecutable checks fail closed; parent-FD spoof remains raw stdout; detached descendants cease. Native SDK timeout control observes a child then confirms none survives terminal `execution_unusable`. |
| G1/G2/#17 compatibility and stop boundary | Same graph/role/route/bound comparison, profile identities, P2 correlation/replay regression suite, package/schema boundary tests and independent reviews. No #19 custody/observation or P4 code. |

The SDK record has **12 cases and 14/14 checks PASS**. Eleven cases use the exact
product graph/runtime. The twelfth explicitly shortens only initial evidence's
timeout to 1000 ms to exercise native cessation; that graph delta is retained.
The same leaf/launcher/Bubblewrap software identities appear in all cases and both
real reviewer runs. Source hashes match the retained implementation and harness.

Regression validation is split without omitting coverage: [248 tests and 1833
subtests](evidence/issue-18-suite.txt), plus [four SDK assurance tests and 47
subtests](evidence/issue-18-assurance-regressions.txt). The separate evidence harness
executes the same scenarios/assertions as `test_evidence_graph.py` and retains the
raw results. The [without-SDK suite](evidence/issue-18-without-sdk.txt) verifies lazy
SDK import and ordinary package use. Ruff passes on changed Python files.

## Configuration and limits

- Zeroshot source `d0909615d6ba3c179b58bce15a059f40400ec995`, SDK `zeroshot-rust 0.1.0.dev0`; exact installed adapter/sidecar identity is in both evidence records.
- Product graph SHA-256 `028d79381aab0cc98950e3eccd2d30a67f25e3e6f836917f5d76310bc9dc2604`.
- Runtime SHA-256 `05fd5af801591caf55464b20f7cc86d98abb20a6b38adf9f45671300342abad0`.
- Codex `0.153.4`, `gpt-5.6-sol`, low effort, execution-scoped; W4 reviewer isolation settings unchanged.
- Evidence leaf SHA-256 `c878f7b8eb1e00f115879e78118edc65c323fe4605ffb9743d1b903d4022410c`; launcher `a9ba410c2ae51d1fc7a0483254753749a70991ae919648278d80631dfece643e`; Bubblewrap `573236e5328ac2ebb08f59ae3a9805b4f8d12bdef14be8af4450d5463294985f`.

The deterministic substitution adds an explicit Bubblewrap launch profile, covered
by its affected containment controls. The supported material is complete UTF-8 text;
unsupported binary material fails closed. Codes 0–127 are observations; 128+ is
reserved as abnormal/unavailable because Bubblewrap encodes signal termination in
that range. No observation decides semantic truth. Controlled semantic roles prove
integration/discrimination; two actual reviewer occurrences prove this bounded
profile, not broad model reliability. Host read-only containment is not a general
confidentiality boundary. No credentials or runtime history archive are retained.

Parent review independently checked canonical Contract preservation, exact graph
changes and source identities. Delegated adversarial review found trusted code
could reside inside a candidate; profile preflight now rejects that configuration.
Parent review found collector PID exposure and newline normalization; the collector
is now outside the sandbox and reads raw bytes. Adversarial spoof/containment tests
pass after those corrections. No unresolved boundary question remains for #18.
