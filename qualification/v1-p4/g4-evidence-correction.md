# Prospective G4 evidence correction review

Baseline: `f4d3a7d8843d0f4669c08157f6b8607323be77df`.
This is plan/backlog review, not G4 gate execution or a gate verdict.

The owner authorized a minimal evidence correction if target architecture and
meaningful P4 assurance remain intact. The
[prospective plan addendum](../../docs/governing/v0.5-g4-evidence-addendum.md)
is the resulting governing change. Target v0.5, original plan v0.5 and all
historical qualification records remain unchanged. The README exposes the
addendum, and preserved before/proposed issue bodies capture the exact backlog
change in [the evidence directory](evidence/g4-evidence-correction).

A fresh independent read-only requirements reviewer found the single all-real
run conjunction to be plan/backlog evidence strategy, not a target invariant.
It inspected raw clean and seeded-repair evidence and the diagnostic shim. It
confirmed compatible graph/runtime, SDK/sidecar, launcher/containment/evidence
leaf, provider version, source placement and durable stores. The shim is an
explicit test-adapter difference: only initial implement is controlled; every
downstream role delegates original prompts/arguments to the same actual CLI
inside the unchanged profile. The shim is not reclassified as an all-real
qualified executable. No meaningful P4 assurance gap was identified from this
composition; general semantic reliability remains unclaimed.

The same reviewer checked the completed addendum, both proposed issue bodies
and all composition evidence hashes. It found no blocking issue. Its optional
clarification was incorporated: deterministic controls and compatibility review
remain mandatory even when one all-real run supplies the semantic witness.

A separate read-only #23 acceptance reviewer verified non-witness criteria
against source and retained actual controls: all 87 product/test and 20 evidence
blobs fetched from origin/main match the verification manifest, and all 94
current control-source hashes match. All 19 dedicated scenarios pass on stable
source. It found no product-spec gap or deferred machinery. Its acceptance
mapping is carried into a separate prospective completion audit; this document
does not itself close #23.

The [parent composition audit](evidence/g4-evidence-correction/composition-audit.json)
verifies shared product graph/runtime and integration, compatible profile fields,
the same frozen CSV Contract for the real semantic legs, exact source/evidence
hashes, current schema/custody and post-cleanup readback. Different Attempt,
source/material and test-adapter identities remain explicit; no in-product
semantic authority is combined across runs.

No new provider attempt, product/test change, profile expansion or regression
rerun was needed. `git diff --check` passes. Earlier blocked records and false
witness predicates remain byte-identical. #24 stays unstarted until amended #23
is genuinely complete, published and closed; its fresh gate must independently
assess every current obligation and preserve BLOCKED/FAIL if any is unmet.
